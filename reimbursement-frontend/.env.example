# Frontend — Environment Variables
# Copy this to .env.local and fill in values

# If you're NOT using Vite's proxy (running frontend/backend on different origins):
# VITE_API_BASE_URL=http://localhost:3000/api/v1

# With Vite proxy (default) — leave empty, /api is proxied automatically
